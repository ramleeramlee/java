1.  int x = 100;
    byte y = (byte)x;
2. 참조형 형변환은 상속 관계일 때만 가능하다
3. 부모클래스 = 수퍼클래스
4. 자식클래스 = 서브클래스
5. 상속을 사용할 때 키워드 = extends
6. 정수(int), 실수(double), 한글자(char), 논리(boolean)
7. 배열, 클래스
8. t = (Truck)c;
9. 기본형 변수에는 값 / 참조형 변수에는 위치를 나타내는 주소가 들어있다
10. null 사용해야함(수동으로 해주어야 함)
11. null = 주소가 없다
12. 포장클래스(랩퍼클래스) = 기본형을 클래스로 만들어놓은 것(Integer, Double, Boolean, Character)
13. 업캐스팅 = 부모클래스로 자식클래스 타입을 변환(Object(큰) <-- String(작))  자동형변환
14. 다운캐스팅 = 자식클래스로 부모클래스 타입을 변환(String(작) <-- Object(큰)) 강제형변환 
 
		